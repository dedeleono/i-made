import styles from './Toolbar.module.css';

const Toolbar = (props) => {
    return(
        <header className={styles.Toolbar}>
            <nav>
                
            </nav>
        </header>
    )
}

export default Toolbar;